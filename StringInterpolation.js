/* String Interpolation= It's like filling in blanks in a sentence with actual values, 
its allowing variables and expressions to be directly inserted into the string*/

const name = "Sarang";
const message = `My name is ${name} .`;
console.log(message);